providers = ["php"]

[variables]
    # This is the only instruction Nixpacks needs to set up Nginx correctly.
    NIXPACKS_PHP_ROOT_DIR = "/app/public"

[phases.install]
    cmds = ["composer install --ignore-platform-reqs --no-interaction", "npm ci"]

[phases.build]
    cmds = ["npm run build"]
