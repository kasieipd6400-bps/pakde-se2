[phases.setup]
nixPkgs = ["nodejs-22_x", "php83", "composer"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader", "npm ci"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
# Use PHP built-in server for simplicity
cmd = "php -S 0.0.0.0:${PORT:-8000} -t public"

