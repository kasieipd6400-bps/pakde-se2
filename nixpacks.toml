providers = ["php"]

[phases.setup]
nixPkgs = ["php83", "nodejs_22", "nginx"]

[phases.install]
cmds = ["composer install --ignore-platform-reqs", "npm ci"]

[phases.build]
# ONLY build the assets here. Do not touch system files.
cmds = ["npm run build"]

[start]
# This is where we handle the Nginx conflict. 
# It runs as the container starts, which has the necessary permissions.
cmd = "rm -f /etc/nginx/conf.d/default.conf && php artisan migrate --force && php-fpm -D && nginx -g 'daemon off;'"

[variables]
NIXPACKS_PROVIDERS = "php"
NIXPACKS_PHP_ROOT_DIR = "/app/public"
IS_STATIC_SITE = "false"
